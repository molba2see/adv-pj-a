# ---- 필수 ----
torch>=2.1.0
transformers>=4.40.0
datasets>=2.18.0
accelerate>=0.28.0
sentencepiece>=0.1.99          # ke-t5 토크나이저에 필수
protobuf>=3.20.0               # sentencepiece 변환 시 필요
pandas>=2.0.0
pyarrow>=14.0.0                # 증강 결과 parquet 캐시
numpy>=1.24.0
scikit-learn>=1.3.0            # TF-IDF 검색기

# ---- LoRA ----
peft>=0.10.0

# ---- 선택 ----
sacrebleu>=2.4.0               # BLEU / chrF
sentence-transformers>=2.6.0   # --retriever dense 사용 시
tensorboard                    # 학습 곡선
kiwipiepy>=0.17.0              # 형태소 분석 (사전 정렬 정확도 향상, retriever.py 참고)
